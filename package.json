{
  "name": "knksongs",
  "private": true,
  "type": "module",
  "scripts": {
    "build:songs-json": "node scripts/build-songs-json.mjs",
    "test:unit": "node --test tests/*.mjs",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}
